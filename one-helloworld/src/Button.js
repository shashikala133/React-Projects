import React from "react";

const Button = ({title="nothing"}) =>(
 <div>
    <button className="button1">{title}</button>
 </div>
)

export default Button;
//if you use curly braces then you have to use return otherwise no need